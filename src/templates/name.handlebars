<div class="ia-single--name {{#ne_and dev_milestone 'live' 'deprecated'}}{{#if permissions.can_edit}}{{#unless permissions.admin}}fix-height{{/unless}}{{/if}}{{/ne_and}}">
    {{#if permissions.admin}}
        {{#eq_or dev_milestone 'live' 'deprecated'}}
            <h2>{{name}}</h2>
        {{else}}
            <input type="text" class="frm__input name top-details js-autocommit" id="name-input" value="{{name}}" />
        {{/eq_or}}
    {{else}}
        <h2>{{name}}</h2>
    {{/if}}
</div>
