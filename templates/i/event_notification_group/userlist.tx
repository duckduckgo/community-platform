<: for results_event_userlist($_.event_notifications) -> $user { :>
  <: if $~user.is_last && $~user.count > 1 { :>
    and
  <: } :>
  <b><: i($user,'username') -:></b>
  <:- if !$~user.is_last && $~user.count > 1 { :>,<: } :>
<: } :>